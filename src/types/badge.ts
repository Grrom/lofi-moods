export default class Badge {
  iconsrc: string;
  name: string;
  id: string;

  constructor(iconsrc: string, name: string, id: string) {
    this.iconsrc = iconsrc;
    this.name = name;
    this.id = id;
  }
}
